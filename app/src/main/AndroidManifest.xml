<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.windows.utscustomlistview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPersegi" />
        <activity android:name=".MainHasil" />
        <activity android:name=".MainLingkaran" />
        <activity android:name=".MainSegitiga" />
        <activity android:name=".HasilLingkaran" />
        <activity android:name=".MainPersegiPanjang" />
        <activity android:name=".HasilSegitiga" />
        <activity android:name=".HasilPersegiPanjang" />
        <activity android:name=".MainTrapesium" />
        <activity android:name=".HasilTrapesium"></activity>

    </application>

</manifest>